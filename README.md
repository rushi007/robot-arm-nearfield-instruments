# Robot Arm — Nearfield Instruments

Welcome to the **Robot Arm project** 

A Python-based implementation of a Robot Arm navigation system that processes movement instructions from an input file, 
validates them, and computes the resulting visited coordinates within a defined boundary.

This project demonstrates clean modular design, file-based execution, boundary validation, and automated testing using Pytest.

---

## 📂 Repository Structure

```text
robot-arm-nearfield-instruments/
│
├── app/
│   ├── __init__.py
│   ├── main.py                # Application entrypoint
│   └── file_parser.py         # Parses and validates system input file
│
├── robot_arm/
│   ├── __init__.py
│   └── robot_arm.py           # Core robot arm logic & movement handling
│
├── robot_arm_tests/
│   ├── helpers/
│   │   ├── __init__.py
│   │   └── file_parser.py     # Test-specific file utilities
│   │
│   ├── resources/             # Test input files
│   │   ├── system_input_all_points_within_bound.txt
│   │   ├── system_input_mix_coords.txt
│   │   ├── system_input_with_empty_points.txt
│   │   ├── system_input_with_invalid_boundry.txt
│   │   ├── system_input_with_invalid_boundry_ccords.txt
│   │   ├── system_input_with_outside_points.txt
│   │   ├── system_input_without_boundry_rectangle.txt
│   │   └── system_input_invalid_file_type.pdf
│   │
│   ├── output/                # Test execution outputs
│   │
│   ├── tests/
│   │   ├── __init__.py
│   │   ├── conftest.py        # Pytest fixtures
│   │   └── test_robot_arm.py  # End-to-end functional tests
│   │
│   └── __init__.py
│
├── system_output/             # Output files generated by application run
│
├── pytest.ini
├── requirements.txt
└── README.md
```

## Key Components

    app/ – Application entrypoint and input parsing

    robot_arm/ – Core robot movement and boundary handling logic

    robot_arm_tests/ – Test suite, fixtures, and test resources

    system_output/ – Output files generated during application execution

---

## 🚀 Features

✔ Modular architecture with separation of concerns 
✔ Deterministic coordinate tracking
✔ Input validation and boundary enforcement  
✔ End-to-end functional testing with Pytest

---

## 🧠 Getting Started

### 📦 Prerequisites

This project requires

**Python 3.8+**

**pip**


It has been tested with standard Python interpreters on macOS.

Install the project dependencies:

```bash
pip install -r requirements.txt
```

### 🏁 Running the Application
The program expects exactly one input file containing:

- A boundary rectangle definition # For example: `(-4, -150), (-4, 150), (160, -150), (160, 150)`
- A list of navigation points # For example: `(-4, -150)`

Run the application as follows:

```bash
python -m app.main <path_to_input_file>
```

For Example:

```bash
python -m app.main ./robot_arm_tests/resources/system_input_file.txt
```
